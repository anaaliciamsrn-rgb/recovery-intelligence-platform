# repositories

Interfaces (ports) de persistência para agregados de domínio (ex.: `IPessoaRepository`). Definidas aqui, implementadas em `infrastructure/database` usando Prisma.

Vazio nesta sprint: nenhuma entidade de negócio existe ainda para ter um repositório.
