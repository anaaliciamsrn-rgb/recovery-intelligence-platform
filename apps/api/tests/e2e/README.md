# e2e

Testes ponta a ponta contra a aplicação containerizada (via `docker compose`), exercitando fluxos completos de negócio.

Vazio nesta sprint: sem regra de negócio para testar de ponta a ponta ainda. O `GET /api/v1/health` já é coberto em `tests/integration`.
